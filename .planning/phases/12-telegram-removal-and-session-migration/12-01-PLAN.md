---
phase: 12-telegram-removal-and-session-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/bot/bot.go
  - internal/bot/handlers.go
  - internal/bot/middleware.go
  - internal/bot/middleware_test.go
  - internal/handlers/callback.go
  - internal/handlers/callback_integration_test.go
  - internal/handlers/callback_safety_test.go
  - internal/handlers/callback_test.go
  - internal/handlers/command.go
  - internal/handlers/command_test.go
  - internal/handlers/document.go
  - internal/handlers/document_test.go
  - internal/handlers/gsd.go
  - internal/handlers/gsd_test.go
  - internal/handlers/helpers.go
  - internal/handlers/helpers_test.go
  - internal/handlers/media_group.go
  - internal/handlers/media_group_test.go
  - internal/handlers/photo.go
  - internal/handlers/photo_test.go
  - internal/handlers/streaming.go
  - internal/handlers/text.go
  - internal/handlers/text_test.go
  - internal/handlers/voice.go
  - internal/handlers/voice_test.go
  - internal/formatting/markdown.go
  - internal/formatting/markdown_test.go
  - internal/formatting/tools.go
  - internal/formatting/tools_test.go
  - internal/security/channel_auth.go
  - internal/security/channel_auth_test.go
  - internal/project/mapping.go
  - internal/project/mapping_test.go
  - internal/config/config.go
  - main.go
  - go.mod
  - go.sum
  - src/autodoc.ts
  - src/config.ts
  - src/formatting.ts
  - src/index.ts
  - src/registry.ts
  - src/security.ts
  - src/session.ts
  - src/types.ts
  - src/utils.ts
  - src/vault-search.ts
  - src/handlers/
  - package.json
  - package-lock.json
  - tsconfig.json
  - bun.lock
  - mcp-config.ts
  - mcp-config.example.ts
  - vitest.config.ts
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "grep -r 'gotgbot\\|openai\\|telegram' internal/ returns zero matches"
    - "go.mod does not reference gotgbot/v2 or openai-go"
    - "No TypeScript files, package.json, tsconfig.json, bun.lock, or npm artifacts exist"
    - "go build ./... succeeds with no Telegram-dependent packages"
    - "go test ./... passes for all remaining packages"
  artifacts:
    - path: "internal/config/config.go"
      provides: "Config without Telegram fields — only ClaudeCLIPath, WorkingDir, AllowedPaths, DataDir, AuditLogPath, RateLimitEnabled/Requests/Window, SafetyPrompt"
    - path: "main.go"
      provides: "Minimal entry point that loads config and exits (placeholder for Phase 13 wiring)"
  key_links:
    - from: "main.go"
      to: "internal/config"
      via: "config.Load()"
      pattern: "config\\.Load"
---

<objective>
Delete all Telegram bot code, TypeScript source, and external dependencies (gotgbot, openai-go) from the repository. Strip Telegram-specific fields from config. Leave a compilable codebase with only the packages needed for the WebSocket node.

Purpose: Phase 13 (dispatch/instance management) needs a clean codebase with no legacy Telegram coupling. Every deleted package is only imported by other deleted packages — no downstream breakage.

Output: Compilable Go module with internal/claude, internal/config, internal/connection, internal/protocol, internal/session, internal/audit, internal/security (ratelimit + validate only) remaining. No TypeScript artifacts.
</objective>

<execution_context>
@C:/Users/jtayl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jtayl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Telegram packages, TypeScript files, and strip dependencies</name>
  <files>
    internal/bot/ (delete entire directory)
    internal/handlers/ (delete entire directory)
    internal/formatting/ (delete entire directory)
    internal/security/channel_auth.go (delete)
    internal/security/channel_auth_test.go (delete)
    internal/project/ (delete entire directory)
    src/ (delete entire directory)
    package.json (delete)
    package-lock.json (delete)
    tsconfig.json (delete)
    bun.lock (delete)
    mcp-config.ts (delete)
    mcp-config.example.ts (delete)
    vitest.config.ts (delete)
    go.mod (modify)
    go.sum (modify)
  </files>
  <read_first>
    go.mod
    internal/security/ratelimit.go
    internal/security/validate.go
  </read_first>
  <action>
1. Delete these entire directories and their contents:
   - `internal/bot/` (bot.go, handlers.go, middleware.go, middleware_test.go)
   - `internal/handlers/` (all 25 files — callback, command, document, gsd, helpers, media_group, photo, streaming, text, voice and their tests)
   - `internal/formatting/` (markdown.go, markdown_test.go, tools.go, tools_test.go)
   - `internal/project/` (mapping.go, mapping_test.go)

2. Delete Telegram-specific files from security:
   - `internal/security/channel_auth.go`
   - `internal/security/channel_auth_test.go`
   Keep: `ratelimit.go`, `ratelimit_test.go`, `validate.go`, `validate_test.go`

3. Delete all TypeScript/Bun/npm files:
   - `src/` directory (all .ts files and src/handlers/ subdirectory)
   - `package.json`, `package-lock.json`, `tsconfig.json`, `bun.lock`
   - `mcp-config.ts`, `mcp-config.example.ts`, `vitest.config.ts`

4. Edit `go.mod`: Remove the `github.com/PaulSonOfLars/gotgbot/v2 v2.0.0-rc.34` line from the require block. There is no openai-go dependency currently in go.mod (it was already removed or never added to Go), so just remove gotgbot.

5. Run `go mod tidy` to clean up go.sum and any transitive dependencies that are no longer needed.

6. Verify `internal/security/validate_test.go` — it matched a grep for "telegram" but likely just has a test string. Read it and remove any Telegram-specific test cases or imports. If it only references "telegram" in a test string value (not an import), leave it as-is — string content is fine.
  </action>
  <verify>
    <automated>cd C:/DATA/CODE/gsd-tele && grep -r "gotgbot\|openai" internal/ go.mod; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `ls internal/bot/ 2>/dev/null` returns nothing (directory deleted)
    - `ls internal/handlers/ 2>/dev/null` returns nothing (directory deleted)
    - `ls internal/formatting/ 2>/dev/null` returns nothing (directory deleted)
    - `ls internal/project/ 2>/dev/null` returns nothing (directory deleted)
    - `ls internal/security/channel_auth.go 2>/dev/null` returns nothing
    - `ls src/ package.json tsconfig.json 2>/dev/null` returns nothing
    - `grep -c "gotgbot" go.mod` returns 0
    - `ls internal/security/ratelimit.go internal/security/validate.go` succeeds (kept)
  </acceptance_criteria>
  <done>All Telegram packages, TypeScript source, and gotgbot dependency are removed. Only WebSocket-node packages remain in internal/.</done>
</task>

<task type="auto">
  <name>Task 2: Strip Telegram config fields and rewrite main.go</name>
  <files>
    internal/config/config.go
    main.go
  </files>
  <read_first>
    internal/config/config.go
    internal/config/node_config.go
    main.go
  </read_first>
  <action>
1. Edit `internal/config/config.go`:
   - Remove `TelegramToken string` field from Config struct
   - Remove `AllowedUsers []int64` field from Config struct
   - Remove `OpenAIAPIKey string` field from Config struct
   - Remove `PdfToTextPath string` field from Config struct
   - Remove all Telegram-specific constants: `TelegramMessageLimit`, `TelegramSafeLimit`, `StreamingThrottleMs`, `ButtonLabelMaxLength`, `QueryTimeoutMs`
   - Keep: `MaxSessionHistory`, `SessionQueueSize`, `BlockedPatterns`
   - Keep all remaining config fields: `WorkingDir`, `ClaudeCLIPath`, `AllowedPaths`, `RateLimitEnabled`, `RateLimitRequests`, `RateLimitWindow`, `AuditLogPath`, `DataDir`, `SafetyPrompt`
   - In `Load()` function: Remove the TELEGRAM_BOT_TOKEN parsing block (lines ~97-99), remove the TELEGRAM_ALLOWED_USERS parsing block (lines ~102-120), remove the OPENAI_API_KEY line (~151), remove the PDFTOTEXT_PATH block (~154-157)
   - The `Load()` function should no longer return error for missing TELEGRAM_BOT_TOKEN or TELEGRAM_ALLOWED_USERS — those are gone
   - Update the package doc comment: change "Telegram bot" to "gsd-tele-go node"
   - Update `BuildSafetyPrompt` — remove "TELEGRAM BOT" from the safety prompt string, replace with "GSD NODE". Remove the line about "running via Telegram" at the bottom.
   - Rename default audit log from "claude-telegram-audit.log" to "gsd-node-audit.log"

2. Rewrite `main.go` to be a minimal placeholder:
   - Keep the signal handling pattern (context + SIGINT/SIGTERM)
   - Load config via `config.Load()`
   - Log startup with `working_dir` and `claude_path`
   - Remove `bot.New(cfg)` and `b.Start(ctx)` and `b.Stop()` — no bot package exists
   - Add a TODO comment: `// TODO(phase-13): Wire ConnectionManager and dispatch loop here`
   - The main function should: load config, log startup info, wait for signal, log shutdown, exit
   - Remove the import of `internal/bot`

3. Run `go build ./...` to verify compilation. Run `go test ./...` to verify all remaining tests pass.
  </action>
  <verify>
    <automated>cd C:/DATA/CODE/gsd-tele && go build ./... && go test ./... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TelegramToken" internal/config/config.go` returns 0
    - `grep -c "AllowedUsers" internal/config/config.go` returns 0
    - `grep -c "OpenAIAPIKey" internal/config/config.go` returns 0
    - `grep -c "TELEGRAM" internal/config/config.go` returns 0
    - `grep -c "internal/bot" main.go` returns 0
    - `grep "TODO(phase-13)" main.go` matches
    - `go build ./...` exits 0
    - `go test ./...` exits 0
  </acceptance_criteria>
  <done>Config struct contains only node-relevant fields. main.go compiles as a minimal entry point. All remaining tests pass. Zero Telegram references in internal/.</done>
</task>

</tasks>

<verification>
1. `grep -r "gotgbot\|openai\|telegram" internal/` returns no matches (case-sensitive; "telegram" in audit log path was renamed)
2. `grep "gotgbot" go.mod` returns no matches
3. `ls src/ package.json tsconfig.json bun.lock 2>/dev/null` returns nothing
4. `go build ./...` succeeds
5. `go test ./...` succeeds with all remaining packages passing
6. `ls internal/` shows only: audit, claude, config, connection, protocol, security, session
</verification>

<success_criteria>
- Zero Telegram/gotgbot/openai imports anywhere in the Go codebase
- No TypeScript or npm/Bun artifacts remain
- go build and go test pass cleanly
- Config struct has no Telegram-specific fields
- main.go compiles without importing internal/bot
</success_criteria>

<output>
After completion, create `.planning/phases/12-telegram-removal-and-session-migration/12-01-SUMMARY.md`
</output>
